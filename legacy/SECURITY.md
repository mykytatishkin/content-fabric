# Security Policy

## 🔒 Политика безопасности

Content Fabric серьезно относится к безопасности. Мы ценим усилия исследователей безопасности и приветствуем ответственное раскрытие уязвимостей.

---

## 📋 Поддерживаемые версии

| Версия | Поддержка безопасности |
|--------|----------------------|
| 2.0 (main branch) | ✅ Активная поддержка |
| 1.x | ⚠️ Только критические исправления |
| < 1.0 | ❌ Не поддерживается |

**Текущая версия**: 2.0 (в разработке, main branch)  
**Статус**: Активная разработка, последняя актуальная версия в main branch

> ⚠️ **Важно**: Ветки обновляются быстро, поэтому для стабильной версии дождитесь первого релиза.

---

## 🚨 Сообщение об уязвимостях

### Как сообщить

Если вы обнаружили уязвимость безопасности, пожалуйста, **НЕ** создавайте публичный issue. Вместо этого:

1. **Telegram**: [@mykytatishkin](https://t.me/mykytatishkin)
2. **Email**: mykyta.tishkin@outlook.com

### Что включить в отчет

- **Описание уязвимости**: Детальное описание проблемы
- **Шаги для воспроизведения**: Пошаговая инструкция
- **Потенциальное воздействие**: Оценка серьезности
- **Предложения по исправлению**: Если есть идеи
- **Доказательство концепции**: Если возможно (без эксплуатации)

### Процесс обработки

1. **Подтверждение** (в течение 48 часов): Мы подтвердим получение отчета
2. **Оценка** (в течение 7 дней): Оценим серьезность и воздействие
3. **Исправление** (в зависимости от серьезности):
   - **Критическая**: 24-48 часов
   - **Высокая**: 7 дней
   - **Средняя**: 30 дней
   - **Низкая**: В следующем релизе
4. **Раскрытие**: После исправления (с вашего разрешения)

---

## 🔐 Рекомендации по безопасности

### Хранение секретов

#### ✅ Правильно

```bash
# Используйте .env файл (добавлен в .gitignore)
YOUTUBE_MAIN_CLIENT_ID=your_client_id
YOUTUBE_MAIN_CLIENT_SECRET=your_client_secret
TELEGRAM_BOT_TOKEN=your_bot_token
```

#### ❌ Неправильно

```python
# НЕ храните секреты в коде
CLIENT_ID = "your_client_id"  # ❌
CLIENT_SECRET = "your_secret"  # ❌
```

### Управление токенами

1. **Регулярное обновление**: Обновляйте access tokens регулярно
2. **Ограничение доступа**: Используйте минимально необходимые OAuth scopes
3. **Ротация**: Регулярно ротируйте credentials
4. **Мониторинг**: Отслеживайте использование токенов

```bash
# Проверка токенов
python run_youtube_manager.py check-tokens

# Переавторизация при необходимости
python reauth_multiple_channels.py --expired
```

### База данных

1. **Пароли**: Используйте сильные пароли для MySQL
2. **Доступ**: Ограничьте доступ к базе данных (только необходимые IP)
3. **Шифрование**: Используйте SSL для подключений к БД
4. **Бэкапы**: Регулярно создавайте резервные копии

```bash
# Пример безопасной конфигурации MySQL
MYSQL_USER=content_fabric_user
MYSQL_PASSWORD=strong_random_password_here
MYSQL_HOST=localhost  # Ограничьте доступ
```

### Логирование

#### ✅ Правильно

```python
logger.info("Task processed successfully")
logger.error(f"Failed to process task: {error_message}")
```

#### ❌ Неправильно

```python
# НЕ логируйте секреты
logger.info(f"Token: {access_token}")  # ❌
logger.debug(f"Password: {password}")  # ❌
```

### Файловая система

1. **Права доступа**: Ограничьте доступ к конфигурационным файлам
   ```bash
   chmod 600 .env
   chmod 600 credentials.json
   ```

2. **Хранение токенов**: Токены хранятся в защищенной директории
   ```bash
   chmod 700 data/tokens/
   ```

3. **Временные файлы**: Очищайте временные файлы после использования

### Сетевая безопасность

1. **HTTPS**: Используйте HTTPS для всех API вызовов
2. **Firewall**: Ограничьте входящие соединения
3. **VPN**: Используйте VPN для удаленного доступа к серверу

---

## 🛡️ Известные уязвимости

### Исправленные

| CVE | Описание | Версия | Дата |
|-----|---------|--------|------|
| - | Нет известных уязвимостей | - | - |

### В работе

| CVE | Описание | Статус | Ожидаемое исправление |
|-----|---------|--------|----------------------|
| - | Нет известных уязвимостей | - | - |

---

## 🔍 Проверка безопасности

### Чеклист перед развертыванием

- [ ] Все секреты в `.env` (не в коде)
- [ ] `.env` добавлен в `.gitignore`
- [ ] `credentials.json` не в репозитории
- [ ] Сильные пароли для MySQL
- [ ] Ограниченный доступ к базе данных
- [ ] Регулярные обновления зависимостей
- [ ] Логи не содержат секретов
- [ ] Правильные права доступа к файлам
- [ ] SSL для подключений к БД
- [ ] Регулярные бэкапы

### Проверка зависимостей

```bash
# Проверка уязвимостей в зависимостях
pip install safety
safety check

# Или используйте pip-audit
pip install pip-audit
pip-audit
```

### Аудит кода

```bash
# Статический анализ кода
pip install bandit
bandit -r core/ app/ scripts/

# Проверка безопасности
pip install semgrep
semgrep --config=auto .
```

---

## 📝 Best Practices

### Разработка

1. **Не коммитьте секреты**: Всегда проверяйте перед коммитом
2. **Используйте переменные окружения**: Для всех конфигураций
3. **Валидация входных данных**: Проверяйте все пользовательские входы
4. **Обработка ошибок**: Не раскрывайте детали ошибок пользователям
5. **Логирование**: Логируйте важные события, но не секреты

### Развертывание

1. **Production secrets**: Используйте менеджеры секретов (HashiCorp Vault, AWS Secrets Manager)
2. **Изоляция**: Запускайте в изолированной среде
3. **Мониторинг**: Настройте мониторинг безопасности
4. **Обновления**: Регулярно обновляйте зависимости и систему

### OAuth и API

1. **Минимальные scopes**: Запрашивайте только необходимые разрешения
2. **Refresh tokens**: Используйте refresh tokens для обновления
3. **Rate limiting**: Соблюдайте лимиты API
4. **Валидация токенов**: Проверяйте валидность токенов перед использованием

---

## 🔄 Обновления безопасности

### Как получать обновления

1. **Следите за репозиторием**: Регулярно обновляйте код
2. **Подпишитесь на уведомления**: GitHub notifications
3. **Проверяйте CHANGELOG**: Перед обновлением

### Процесс обновления

```bash
# 1. Создайте бэкап
cp -r . ../content-fabric-backup

# 2. Обновите код
git pull origin main

# 3. Обновите зависимости
pip install -r requirements.txt --upgrade

# 4. Проверьте конфигурацию
python app/main.py status

# 5. Проверьте базу данных
python run_setup_database.py
```

---

## 📞 Контакты

### Безопасность

- **Telegram**: [@mykytatishkin](https://t.me/mykytatishkin)
- **GitHub**: (через приватные сообщения)

### Общие вопросы

- **Документация**: [DOCS_INDEX.md](DOCS_INDEX.md)
- **Issues**: GitHub Issues (для не-безопасных вопросов)

---

## 📜 Лицензия

Этот документ является частью проекта Content Fabric и распространяется под той же лицензией, что и основной проект (MIT License).

---

## 🙏 Благодарности

Мы благодарим всех исследователей безопасности, которые помогают сделать Content Fabric более безопасным, ответственно раскрывая уязвимости.

---

**Последнее обновление**: 2025-12-24  
**Версия политики**: 1.0
