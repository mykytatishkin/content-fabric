# 🔐 Руководство по переавторизации YouTube каналов

> **Решение проблем с истекшими и отозванными OAuth токенами**

## 📋 Содержание

- [Проблема](#проблема)
- [Причины истечения токенов](#причины-истечения-токенов)
- [Инструменты для решения](#инструменты-для-решения)
- [Быстрый старт](#быстрый-старт)
- [Детальное использование](#детальное-использование)
- [Диагностика проблем](#диагностика-проблем)
- [Лучшие практики](#лучшие-практики)
- [FAQ](#faq)

---

## 🚨 Проблема

### Симптомы

Вы видите ошибку в логах:

```log
ERROR - Resumable upload error: ('invalid_grant: Token has been expired or revoked.', 
                                 {'error': 'invalid_grant', 
                                  'error_description': 'Token has been expired or revoked.'})
```

**Что это означает:**
- ❌ Refresh token для канала больше не действителен
- ❌ Невозможно загружать видео на этот канал
- ❌ Требуется повторная авторизация

---

## 🔍 Причины истечения токенов

### 1. **Истечение срока действия (6 месяцев)**
```
Refresh token автоматически отзывается если:
• Не использовался более 6 месяцев
• Не проводилась refresh операция
```

### 2. **Ручная отзывка пользователем**
```
Пользователь отозвал доступ в:
Google Account → Security → Third-party apps with account access
```

### 3. **Превышен лимит токенов**
```
Google OAuth лимит:
• 50 refresh tokens на (Client ID + User + Scopes)
• При создании 51-го токена, самый старый отзывается
```

### 4. **Изменение OAuth Consent Screen**
```
Если изменились scopes в Google Cloud Console:
• Все старые токены становятся недействительными
• Требуется повторная авторизация
```

---

## 🛠️ Инструменты для решения

### Доступные скрипты

| Скрипт | Назначение | Когда использовать |
|--------|------------|-------------------|
| `reauth_multiple_channels.py` | Переавторизация каналов | Исправить expired токены |
| `check_token_limit.py` | Проверка лимитов | Диагностика проблем |
| `run_youtube_manager.py check-tokens` | Проверка статуса | Мониторинг |

---

## 🚀 Быстрый старт

### Шаг 1: Диагностика

Проверьте, какие каналы требуют переавторизации:

```bash
python run_youtube_manager.py check-tokens
```

**Пример вывода:**
```
🔑 Статус токенов:
  ✅ channel-1: Токен действителен
  ⚠️  audiokniga-one: Токен истек
  ✅ channel-3: Токен действителен
```

### Шаг 2: Переавторизация

#### Вариант A: Один канал
```bash
python3 reauth_multiple_channels.py audiokniga-one
```

#### Вариант B: Несколько каналов
```bash
python3 reauth_multiple_channels.py audiokniga-one audiokniga-two
```

#### Вариант C: Все каналы с истекшими токенами
```bash
python3 reauth_multiple_channels.py --expired
```

### Шаг 3: Проверка

```bash
python run_youtube_manager.py check-tokens
```

**Успех выглядит так:**
```
🔑 Статус токенов:
  ✅ channel-1: Токен действителен
  ✅ audiokniga-one: Токен действителен  ← Исправлено!
  ✅ channel-3: Токен действителен
```

---

## 📖 Детальное использование

### `reauth_multiple_channels.py`

#### Синтаксис
```bash
python3 reauth_multiple_channels.py [OPTIONS] [CHANNELS...]
```

#### Опции

##### 1. Один канал
```bash
python3 reauth_multiple_channels.py audiokniga-one
```

**Процесс:**
1. ✅ Проверяет существование канала в базе
2. 🌐 Открывает браузер для авторизации
3. 🔑 Получает новые токены
4. 💾 Сохраняет в базу данных

**Вывод:**
```
======================================================================
🔐 Re-authenticating: audiokniga-one
======================================================================

✅ SUCCESS! 'audiokniga-one' re-authenticated
   Access Token: ya29.a0AfB_byBwU6Zq...
   Refresh Token: 1//0gYXqK9vMW8CbC...
```

##### 2. Несколько каналов
```bash
python3 reauth_multiple_channels.py channel-1 channel-2 channel-3
```

**Процесс:**
1. ✅ Валидирует все каналы перед началом
2. 🔄 Переавторизует каждый канал по очереди
3. ⏳ Пауза 5 секунд между каналами
4. 📊 Показывает итоговую статистику

**Вывод:**
```
======================================================================
📊 RE-AUTHENTICATION SUMMARY
======================================================================

✅ Successful: 3/3
   - channel-1
   - channel-2
   - channel-3
```

##### 3. Автоматический поиск истекших
```bash
python3 reauth_multiple_channels.py --expired
```

**Процесс:**
1. 🔍 Ищет все enabled каналы
2. ⚠️  Находит каналы с истекшими токенами
3. ❓ Запрашивает подтверждение
4. 🔄 Переавторизует все проблемные

**Вывод:**
```
🔍 Finding channels with expired tokens...

⚠️  Found 2 channel(s) with expired tokens:
   - audiokniga-one
   - test-channel

❓ Re-authenticate these channels? (yes/no): yes
```

#### Что происходит при авторизации

```
1. Создание OAuth URL
   ↓
2. Запуск локального сервера на :8080
   ↓
3. Открытие браузера
   ↓
4. Пользователь входит в Google
   ↓
5. Пользователь разрешает доступ
   ↓
6. Callback на localhost:8080/callback
   ↓
7. Обмен кода на токены
   ↓
8. Сохранение в базу данных
   ↓
9. Готово! ✅
```

---

### `check_token_limit.py`

#### Назначение
Проверяет, не превышен ли лимит Google OAuth токенов (50 на Client ID + User + Scopes).

#### Использование

##### Базовая проверка
```bash
python3 check_token_limit.py
```

**Вывод:**
```
======================================================================
🔍 GOOGLE OAUTH REFRESH TOKEN LIMIT CHECKER
======================================================================

📊 Found 45 channel(s)

📱 Unique OAuth Applications (Client IDs): 3

──────────────────────────────────────────────────────────────────────
App #1: 123456789-abc.apps.googleusercontent.com...
──────────────────────────────────────────────────────────────────────
Channels using this Client ID: 42

⚠️  WARNING: You have many channels with same Client ID!
   Each re-authorization creates a new refresh token
   Google limit: 50 tokens per Client ID + User + Scopes

Channels:
  ✅ audiokniga-one
     Refresh Token: 🔑
     Token Status: ✅ Valid
     Last Updated: 5 days ago
```

##### Полная информация с решениями
```bash
python3 check_token_limit.py --help
```

**Показывает:**
- 📊 Анализ текущего состояния
- ⚠️  Предупреждения о возможных проблемах
- 💡 Рекомендации по исправлению
- 🔧 Пошаговое решение проблем с лимитами

---

## 🔬 Диагностика проблем

### Проблема 1: "Channel not found in database"

**Ошибка:**
```
❌ Error: Channel 'my-channel' not found in database!
```

**Решение:**
```bash
# 1. Проверьте список каналов
python run_youtube_manager.py list

# 2. Добавьте канал если его нет
python run_youtube_manager.py add-channel \
  --name "my-channel" \
  --channel-id "UCxxxxxxxxxxxxx" \
  --client-id "$YOUTUBE_CLIENT_ID" \
  --client-secret "$YOUTUBE_CLIENT_SECRET"
```

---

### Проблема 2: "Authorization failed"

**Ошибка:**
```
❌ FAILED to re-authenticate channel 'audiokniga-one'
   Error: Ошибка получения токена: invalid_client
```

**Причины и решения:**

#### Причина A: Неверный Client ID/Secret
```bash
# Проверьте credentials
python run_youtube_manager.py show-channel audiokniga-one

# Обновите если неверны
python run_youtube_manager.py update-channel audiokniga-one \
  --client-id "новый_client_id" \
  --client-secret "новый_secret"
```

#### Причина B: Неверный Redirect URI
```bash
# В Google Cloud Console должно быть:
Authorized redirect URIs:
  http://localhost:8080/callback
```

#### Причина C: OAuth Consent Screen не настроен
```
1. Зайдите в Google Cloud Console
2. APIs & Services → OAuth consent screen
3. Добавьте test users (если app в Testing mode)
4. Проверьте scopes:
   - https://www.googleapis.com/auth/youtube.upload
   - https://www.googleapis.com/auth/youtube
   - https://www.googleapis.com/auth/youtube.force-ssl
```

---

### Проблема 3: Browser не открывается

**Решение:**
```bash
# Скрипт покажет URL, скопируйте его вручную
# Откройте URL в браузере самостоятельно
```

---

### Проблема 4: "No refresh token in response"

**Причина:**
- Google не вернул refresh_token

**Решение:**

Код уже настроен правильно (`prompt=consent`, `access_type=offline`), но если проблема осталась:

```bash
# 1. Отзовите доступ вручную:
# https://myaccount.google.com/permissions

# 2. Попробуйте снова
python3 reauth_multiple_channels.py audiokniga-one
```

---

## 🎯 Лучшие практики

### 1. **Регулярный мониторинг**

Проверяйте токены раз в неделю:
```bash
# Добавьте в cron
0 9 * * 1 cd /path/to/content-fabric && python run_youtube_manager.py check-tokens
```

### 2. **Проактивное обновление**

Обновленный код (`youtube_client.py`) автоматически:
- ✅ Обновляет токены за 5 минут до истечения
- ✅ Ловит `invalid_grant` ошибки рано
- ✅ Дает четкие инструкции по исправлению

### 3. **Используйте разные Client IDs**

Если у вас > 40 каналов:
```
Production channels: client_id_1
Test channels: client_id_2
Development: client_id_3
```

### 4. **Не переавторизуйте без необходимости**

```
❌ НЕ ДЕЛАЙТЕ: python3 reauth_multiple_channels.py --expired (каждый день)
✅ ДЕЛАЙТЕ: Переавторизуйте только когда токен действительно истек
```

### 5. **Отслеживайте лимиты**

```bash
# Регулярно проверяйте
python3 check_token_limit.py

# Если предупреждение - действуйте
```

---

## 📊 Примеры использования

### Пример 1: Один канал перестал работать

```bash
# Шаг 1: Проверка
python run_youtube_manager.py check-tokens
# Вывод: ⚠️  audiokniga-one: Токен истек

# Шаг 2: Переавторизация
python3 reauth_multiple_channels.py audiokniga-one

# Шаг 3: Проверка
python run_youtube_manager.py check-tokens
# Вывод: ✅ audiokniga-one: Токен действителен
```

### Пример 2: Массовое истечение токенов

```bash
# Шаг 1: Автоматический поиск
python3 reauth_multiple_channels.py --expired

# Вывод:
# ⚠️  Found 5 channel(s) with expired tokens:
#    - channel-1
#    - channel-2
#    - channel-3
#    - channel-4
#    - channel-5
# 
# ❓ Re-authenticate these channels? (yes/no): yes

# Шаг 2: Авторизация всех
# (процесс идет автоматически)

# Шаг 3: Проверка результата
# ✅ Successful: 5/5
```

### Пример 3: Диагностика лимитов

```bash
# Проверка перед переавторизацией
python3 check_token_limit.py

# Если показывает:
# ⚠️  WARNING: You have 48 channels with same Client ID!
# 
# Решение: Используйте отдельные Client IDs для групп каналов
```

---

## 🔗 Связанные документы

- [04-TROUBLESHOOTING.md](./04-TROUBLESHOOTING.md) - Общее устранение неполадок
- [03-MULTIPLE-CHANNELS.md](./03-MULTIPLE-CHANNELS.md) - Работа с несколькими каналами
- [Google OAuth 2.0 Docs](https://developers.google.com/identity/protocols/oauth2)

---

## ❓ FAQ

### Q: Как часто нужно переавторизовывать каналы?

**A:** При правильной настройке - **никогда**. 

Обновленный код автоматически обновляет токены:
- Access token обновляется каждый час (автоматически)
- Refresh token не истекает, если используется
- Переавторизация нужна только если refresh token отозван

### Q: Потеряю ли я данные при переавторизации?

**A:** **Нет**. 

Переавторизация только обновляет токены доступа:
- ✅ Все видео на канале остаются
- ✅ Статистика сохраняется
- ✅ Настройки канала не меняются
- ✅ История загрузок в базе данных остается

### Q: Можно ли переавторизовать все каналы сразу?

**A:** **Можно, но не рекомендуется**.

```bash
# Работает, но может превысить лимит токенов
python3 reauth_multiple_channels.py channel1 channel2 ... channel50
```

**Лучше:**
```bash
# Переавторизуйте только проблемные
python3 reauth_multiple_channels.py --expired
```

### Q: Что делать если превышен лимит токенов?

**A:** См. раздел "Лимиты" в `check_token_limit.py --help`

Краткое решение:
1. Создайте новые OAuth приложения в Google Cloud Console
2. Распределите каналы по разным Client IDs
3. Обновите credentials в базе данных
4. Переавторизуйте каналы

### Q: Нужно ли останавливать task worker перед переавторизацией?

**A:** **Нет**, но рекомендуется.

- ✅ Можно переавторизовывать "на лету"
- ⚠️  Во время переавторизации task worker может словить ошибку
- 💡 Лучше: остановить worker → переавторизовать → запустить worker

### Q: Как узнать, что токен скоро истечет?

**A:** 

```bash
# Проверка токенов
python run_youtube_manager.py check-tokens

# Обновленный код предупредит за 5 минут в логах:
# INFO - Token expires soon for account audiokniga-one, refreshing...
```

### Q: Можно ли автоматизировать переавторизацию?

**A:** **Частично**.

- ❌ Нельзя полностью автоматизировать (требует действий пользователя в браузере)
- ✅ Можно автоматически определять проблемные каналы
- ✅ Можно настроить алерты при истечении токенов

---

## 📝 Changelog

### 2025-10-21
- ✅ Создан `reauth_multiple_channels.py`
- ✅ Создан `check_token_limit.py`
- ✅ Улучшена обработка токенов в `youtube_client.py`
- ✅ Добавлено проактивное обновление токенов

---

## 🆘 Поддержка

Если проблема не решается:

1. Проверьте логи: `tail -f data/logs/latest.log`
2. Запустите диагностику: `python3 check_token_limit.py`
3. Проверьте Google Cloud Console credentials
4. Убедитесь, что redirect URI настроен правильно

**Типичные ошибки и решения см. в [04-TROUBLESHOOTING.md](./04-TROUBLESHOOTING.md)**

---

**Версия:** 1.0  
**Дата:** 2025-10-21  
**Автор:** Content Fabric Team

