# 📺 YouTube Automation - CLI Guide

Полное руководство по использованию командной строки для управления YouTube каналами.

---

## 📚 Содержание

1. [account_manager.py](#accountmanagerpy) - Основной инструмент
2. [run_youtube_manager.py](#runyoutubemanagerpy) - Управление через базу
3. [Примеры использования](#примеры-использования)
4. [Публикация видео](#публикация-видео)

---

## 🛠️ account_manager.py

Основной инструмент для управления каналами. Автоматически использует credentials из `.env`.

### Путь к файлу
```bash
scripts/account_manager.py
```

### Синтаксис
```bash
python scripts/account_manager.py <команда> [параметры]
```

---

## 📋 Команды account_manager.py

### 1. add-channel - Добавить канал

Добавляет новый YouTube канал в систему.

**Синтаксис:**
```bash
python scripts/account_manager.py add-channel <название> <channel_id> [--auto-auth]
```

**Параметры:**
- `название` - Уникальное имя канала в системе (в кавычках если есть пробелы)
- `channel_id` - ID канала YouTube (формат: `@username` или `UC...`)
- `--auto-auth` - Автоматически запустить авторизацию после добавления

**Примеры:**
```bash
# Добавить канал с автоавторизацией (рекомендуется)
python scripts/account_manager.py add-channel "Мой Канал" "@mychannel" --auto-auth

# Добавить канал без авторизации (авторизуем позже)
python scripts/account_manager.py add-channel "Резервный" "@backup"

# Канал с UC ID
python scripts/account_manager.py add-channel "Production" "UC1234567890abcdef" --auto-auth
```

**Что происходит:**
1. ✅ Создаётся запись в базе данных
2. ✅ Автоматически берёт `YOUTUBE_MAIN_CLIENT_ID` и `YOUTUBE_MAIN_CLIENT_SECRET` из `.env`
3. 🌐 Если `--auto-auth`: открывается браузер для OAuth
4. ✅ Токены сохраняются в базу данных

**Примечания:**
- ⚠️ Имя канала должно быть уникальным
- ⚠️ Используйте кавычки для имён с пробелами
- ✅ Один OAuth клиент можно использовать для всех каналов
- ✅ Каждый канал может иметь свой Google аккаунт

---

### 2. authorize - Авторизовать канал

Авторизует существующий канал через OAuth 2.0.

**Синтаксис:**
```bash
python scripts/account_manager.py authorize --platform youtube --account <название>
```

**Параметры:**
- `--platform youtube` - Платформа (всегда youtube)
- `--account` - Название канала из базы данных

**Примеры:**
```bash
# Авторизовать канал
python scripts/account_manager.py authorize --platform youtube --account "Мой Канал"

# Переавторизовать канал (если токен истёк)
python scripts/account_manager.py authorize --platform youtube --account "Резервный"
```

**Процесс:**
1. 🌐 Открывается браузер с Google OAuth
2. 🔐 Выберите нужный Google аккаунт
3. ✅ Разрешите доступ к YouTube API
4. 🔄 Автоматическое перенаправление на localhost:8080
5. ✅ Токены сохраняются в базу

**Примечания:**
- ⚠️ Порт 8080 должен быть свободен
- ✅ Refresh token сохраняется автоматически
- ✅ Access token обновляется автоматически при истечении

---

### 3. db - Работа с базой данных

Команды для управления каналами в базе данных.

#### 3.1 db list - Список каналов

**Синтаксис:**
```bash
python scripts/account_manager.py db list
```

**Пример вывода:**
```
📺 Каналы в базе данных:
   Всего: 3
   Включенных: 3
   Авторизованных: 2
   Действительных токенов: 1
   Истекших токенов: 1

   ✅ 🔑 🟢 Мой Канал
   ✅ 🔑 🔴 Резервный
   ✅ 🔒 🔴 Третий Канал
```

**Расшифровка символов:**
- ✅ / ❌ - Канал включен / отключен
- 🔑 / 🔒 - Есть токен / нет токена
- 🟢 / 🔴 - Токен действителен / истёк

#### 3.2 db remove - Удалить канал

**Синтаксис:**
```bash
python scripts/account_manager.py db remove <название>
```

**Пример:**
```bash
python scripts/account_manager.py db remove "Старый Канал"
```

**Что удаляется:**
- ✅ Запись из базы данных
- ✅ Сохранённые токены
- ⚠️ Настройки OAuth клиента НЕ удаляются

---

### 4. migrate - Миграция из config.yaml

Переносит каналы из `config.yaml` в базу данных.

**Синтаксис:**
```bash
python scripts/account_manager.py migrate [--dry-run]
```

**Параметры:**
- `--dry-run` - Предварительный просмотр без изменений

**Примеры:**
```bash
# Предварительный просмотр
python scripts/account_manager.py migrate --dry-run

# Выполнить миграцию
python scripts/account_manager.py migrate
```

**Когда использовать:**
- При переходе от config.yaml к базе данных
- При восстановлении каналов из backup config.yaml

---

## 🗄️ run_youtube_manager.py

Альтернативный инструмент для управления каналами с явным указанием credentials.

### Путь к файлу
```bash
run_youtube_manager.py
```

---

## 📋 Команды run_youtube_manager.py

### 1. add - Добавить канал

Добавляет канал с явным указанием OAuth credentials.

**Синтаксис:**
```bash
python run_youtube_manager.py add <название> \
  --channel-id <id> \
  --client-id <client_id> \
  --client-secret <client_secret> \
  [--disabled]
```

**Параметры:**
- `название` - Имя канала в системе
- `--channel-id` - YouTube channel ID
- `--client-id` - OAuth Client ID
- `--client-secret` - OAuth Client Secret
- `--disabled` - Добавить отключенным

**Пример:**
```bash
python run_youtube_manager.py add "Спец Канал" \
  --channel-id "@special" \
  --client-id "123-xyz.apps.googleusercontent.com" \
  --client-secret "GOCSPX-abc123"
```

**Когда использовать:**
- Если нужны разные OAuth клиенты для разных каналов
- Для каналов с отдельными Google Cloud проектами

---

### 2. list - Список каналов

**Синтаксис:**
```bash
python run_youtube_manager.py list [--enabled-only]
```

**Примеры:**
```bash
# Все каналы
python run_youtube_manager.py list

# Только включенные
python run_youtube_manager.py list --enabled-only
```

**Пример вывода:**
```
📺 YouTube каналы:
  1. ✅ Мой Канал (ID: @mychannel)
     Токен: 🔑 ✅
     Истекает: 2025-10-17 12:00:00
  
  2. ✅ Резервный (ID: @backup)
     Токен: 🔑 ⚠️
     Истекает: 2025-10-16 10:00:00 (ИСТЁК)
```

---

### 3. enable / disable - Включить/отключить канал

**Синтаксис:**
```bash
python run_youtube_manager.py enable <название>
python run_youtube_manager.py disable <название>
```

**Примеры:**
```bash
# Включить канал
python run_youtube_manager.py enable "Резервный"

# Отключить канал
python run_youtube_manager.py disable "Тестовый"
```

**Примечания:**
- Отключенные каналы не используются при публикации
- Токены сохраняются даже у отключенных каналов

---

### 4. delete - Удалить канал

**Синтаксис:**
```bash
python run_youtube_manager.py delete <название>
```

**Пример:**
```bash
python run_youtube_manager.py delete "Старый"
```

---

### 5. show - Информация о канале

**Синтаксис:**
```bash
python run_youtube_manager.py show <название>
```

**Пример:**
```bash
python run_youtube_manager.py show "Мой Канал"
```

**Пример вывода:**
```
📺 Канал: Мой Канал
   ID: @mychannel
   Client ID: 123456789-abc...
   Статус: ✅ Включен
   Токен: 🔑 Есть
   Истекает: 2025-10-17 12:00:00
   Создан: 2025-10-01 10:00:00
   Обновлен: 2025-10-16 11:30:00
```

---

### 6. check-tokens - Проверка токенов

**Синтаксис:**
```bash
python run_youtube_manager.py check-tokens
```

**Пример вывода:**
```
🔑 Статус токенов:
  ✅ Мой Канал: Токен действителен
  ⚠️ Резервный: Токен истек
  ⚠️ Третий: Токен истек
```

**Когда использовать:**
- Регулярная проверка (рекомендуется каждые 6 часов)
- Перед важной публикацией
- При диагностике проблем

---

### 7. export - Экспорт конфигурации

**Синтаксис:**
```bash
python run_youtube_manager.py export [--output файл.json]
```

**Примеры:**
```bash
# Вывести в консоль
python run_youtube_manager.py export

# Сохранить в файл
python run_youtube_manager.py export --output backup.json
```

---

### 8. import - Импорт конфигурации

**Синтаксис:**
```bash
python run_youtube_manager.py import <файл.json>
```

**Пример:**
```bash
python run_youtube_manager.py import backup.json
```

---

## 🎬 Публикация видео

### Через app/main.py

**Базовый синтаксис:**
```bash
python app/main.py post \
  --content <путь_к_видео> \
  --caption "<описание>" \
  --platforms youtube
```

**Примеры:**

#### 1. Публикация на все каналы
```bash
python app/main.py post \
  --content data/content/videos/video.mp4 \
  --caption "Описание видео #shorts" \
  --platforms youtube
```

#### 2. Публикация на конкретные каналы
```bash
python app/main.py post \
  --content data/content/videos/video.mp4 \
  --caption "Описание #shorts" \
  --platforms youtube \
  --accounts "Мой Канал,Резервный"
```

#### 3. Отложенная публикация
```bash
python app/main.py schedule \
  --content data/content/videos/video.mp4 \
  --caption "Отложенное видео #shorts" \
  --platforms youtube \
  --time "2025-10-17T18:00:00"
```

#### 4. С пользовательскими метаданными
```bash
python app/main.py post \
  --content video.mp4 \
  --caption "Описание" \
  --platforms youtube \
  --metadata '{"title": "Заголовок", "privacy_status": "unlisted"}'
```

---

## 📊 Примеры использования

### Сценарий 1: Добавить первый канал

```bash
# 1. Добавить и авторизовать
python scripts/account_manager.py add-channel "Мой Канал" "@mychannel" --auto-auth

# 2. Проверить статус
python scripts/account_manager.py db list

# 3. Опубликовать тест
python app/main.py post \
  --content test.mp4 \
  --caption "Тест #shorts" \
  --platforms youtube
```

### Сценарий 2: Добавить несколько каналов

```bash
# Добавить 3 канала с автоавторизацией
python scripts/account_manager.py add-channel "Канал 1" "@channel1" --auto-auth
python scripts/account_manager.py add-channel "Канал 2" "@channel2" --auto-auth
python scripts/account_manager.py add-channel "Канал 3" "@channel3" --auto-auth

# Проверить все
python scripts/account_manager.py db list
```

### Сценарий 3: Переавторизация канала

```bash
# 1. Проверить токены
python run_youtube_manager.py check-tokens

# 2. Найти истёкшие
# Вывод: ⚠️ Резервный: Токен истек

# 3. Переавторизовать
python scripts/account_manager.py authorize --platform youtube --account "Резервный"

# 4. Проверить снова
python run_youtube_manager.py check-tokens
```

### Сценарий 4: Отключить канал на время

```bash
# Отключить
python run_youtube_manager.py disable "Тестовый"

# Проверить (канал будет с ❌)
python scripts/account_manager.py db list

# Включить обратно
python run_youtube_manager.py enable "Тестовый"
```

### Сценарий 5: Резервное копирование

```bash
# Экспорт конфигурации
python run_youtube_manager.py export --output youtube_backup_$(date +%Y%m%d).json

# Восстановление (если нужно)
python run_youtube_manager.py import youtube_backup_20251016.json
```

---

## 🔍 Полезные команды

### Мониторинг

```bash
# Проверка статуса всех каналов
python scripts/account_manager.py db list

# Проверка токенов
python run_youtube_manager.py check-tokens

# Просмотр логов
tail -f logs/auto_posting.log | grep YouTube
```

### Отладка

```bash
# Детальная информация о канале
python run_youtube_manager.py show "Канал"

# Проверка подключения к базе
python -c "from core.database import get_database_by_type; db = get_database_by_type(); print('OK' if db else 'FAIL')"

# Проверка OAuth конфигурации
cat .env | grep YOUTUBE
ls -la credentials.json
```

---

## 📝 Советы и best practices

### 1. Именование каналов
```bash
# ✅ Хорошо
python scripts/account_manager.py add-channel "Production Main" "@prod_main"
python scripts/account_manager.py add-channel "Test Channel" "@test"

# ❌ Плохо (без кавычек при наличии пробелов)
python scripts/account_manager.py add-channel Production Main "@prod"
```

### 2. Регулярная проверка токенов
```bash
# Настройте cron job (каждые 6 часов)
0 */6 * * * cd /path/to/project && python run_youtube_manager.py check-tokens
```

### 3. Backup конфигурации
```bash
# Еженедельный backup
0 0 * * 0 cd /path/to/project && python run_youtube_manager.py export --output backups/youtube_$(date +\%Y\%m\%d).json
```

### 4. Использование --auto-auth
```bash
# Всегда используйте --auto-auth для новых каналов
python scripts/account_manager.py add-channel "Новый" "@new" --auto-auth
```

---

## 🎯 Чек-лист: Добавление канала

- [ ] Убедитесь, что `.env` настроен с credentials
- [ ] Убедитесь, что `credentials.json` на месте
- [ ] Добавьте Google аккаунт в Test Users (GCP Console)
- [ ] Добавьте канал: `add-channel "Имя" "@id" --auto-auth`
- [ ] Войдите в нужный Google аккаунт в браузере
- [ ] Разрешите доступ к YouTube API
- [ ] Проверьте статус: `db list`
- [ ] Проверьте токен: `check-tokens`
- [ ] Протестируйте публикацию

---

## 📞 Поддержка

При возникновении проблем:

- 📖 [Troubleshooting Guide](04-TROUBLESHOOTING.md)
- 🏗️ [Architecture Guide](03-ARCHITECTURE.md)
- 📝 Проверьте логи: `tail -f logs/auto_posting.log`

---

**Предыдущий:** [← Setup Guide](01-SETUP.md)  
**Следующий:** [Architecture Guide →](03-ARCHITECTURE.md)

