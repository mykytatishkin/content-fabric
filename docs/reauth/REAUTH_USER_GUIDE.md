# 🔐 YouTube OAuth Re-authentication Service - Полное руководство

> Автоматизированная переавторизация YouTube каналов с полной автоматизацией процесса

**Версия:** 2.0.0  
**Дата:** 2025-11-14  
**Ветка:** `rpa-auth`

---

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Когда использовать](#когда-использовать)
3. [Установка и настройка](#установка-и-настройка)
4. [Использование](#использование)
5. [Автоматизация](#автоматизация)
6. [Диагностика и отладка](#диагностика-и-отладка)
7. [Решение проблем](#решение-проблем)
8. [FAQ](#faq)

---

## 🚀 Быстрый старт

### Переавторизация одного канала

```bash
cd /Users/mykytatishkin/Documents/GitHub/content-fabric
MYSQL_HOST=46.21.250.43 MYSQL_PORT=3306 python3 run_youtube_reauth.py --redirect-port 9090 "Channel Name"
```

### Переавторизация всех каналов с истекшими токенами

```bash
python3 run_youtube_reauth.py --all-expiring --redirect-port 9090
```

### Переавторизация нескольких каналов

```bash
python3 run_youtube_reauth.py "Channel 1" "Channel 2" "Channel 3" --redirect-port 9090
```

---

## ⚠️ Когда использовать

### Основные случаи:

1. **Ошибка `invalid_grant: Token has been expired or revoked`**
   ```
   ERROR - invalid_grant: Token has been expired or revoked
   ```

2. **Ошибка `invalid_credentials`**
   ```
   ERROR - invalid_credentials: The provided credentials are invalid
   ```

3. **Токен был отозван вручную** в Google Cloud Console

4. **Профилактическая переавторизация** перед важными операциями

5. **Смена OAuth Client ID/Secret** для канала

### Когда НЕ нужно использовать:

- ✅ Токены обновляются автоматически через refresh token
- ✅ Ошибки временные (network issues, rate limits)
- ✅ Проблемы с API квотами (не связаны с токенами)

---

## 🔧 Установка и настройка

### Предварительные требования

1. **Python 3.10+**
   ```bash
   python3 --version
   ```

2. **Установленные зависимости**
   ```bash
   pip install -r requirements.txt
   ```

3. **MySQL база данных** (запущенная и доступная)
   ```bash
   # Проверка подключения
   mysql -h 46.21.250.43 -P 3306 -u your_user -p
   ```

4. **Настроенные каналы в базе данных**
   - Таблица `youtube_channels` должна содержать канал
   - Таблица `youtube_account_credentials` должна содержать учетные данные

### Настройка переменных окружения

Создайте файл `.env` в корне проекта (опционально):

```bash
# MySQL настройки (если не используете дефолтные)
MYSQL_HOST=46.21.250.43
MYSQL_PORT=3306
MYSQL_USER=your_user
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=your_database

# Telegram уведомления (опционально)
TELEGRAM_BOT_TOKEN=your_token
TELEGRAM_CHAT_ID=your_chat_id
```

### Настройка OAuth в Google Cloud Console

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите ваш проект
3. APIs & Services → Credentials
4. Создайте OAuth 2.0 Client ID (если еще нет)
5. **Важно:** Добавьте Redirect URI:
   ```
   http://localhost:9090/callback
   ```
   (или другой порт, который вы используете)

6. Сохраните `Client ID` и `Client Secret`
7. Добавьте их в базу данных в таблицу `youtube_channels`

---

## 📖 Использование

### Базовый синтаксис

```bash
python3 run_youtube_reauth.py [OPTIONS] [CHANNEL_NAMES...]
```

### Параметры командной строки

| Параметр | Описание | Пример |
|----------|-----------|--------|
| `CHANNEL_NAMES` | Имена каналов для переавторизации | `"Andrew Garle"` |
| `--all-expiring` | Переавторизовать все каналы с истекшими токенами | `--all-expiring` |
| `--redirect-port PORT` | Порт для OAuth callback (по умолчанию 8080) | `--redirect-port 9090` |
| `--headless` | Запустить браузер в headless режиме | `--headless` |
| `--timeout SECONDS` | Таймаут операции (по умолчанию 300) | `--timeout 600` |

### Примеры использования

#### 1. Переавторизация одного канала

```bash
python3 run_youtube_reauth.py "Andrew Garle" --redirect-port 9090
```

**Что происходит:**
1. Скрипт подключается к MySQL базе данных
2. Загружает учетные данные для канала "Andrew Garle"
3. Открывает браузер (Playwright)
4. Автоматически вводит email и пароль (если нужно)
5. Проходит MFA (если настроено)
6. **Автоматически нажимает кнопку "Continue" на consent screen** ✨
7. Получает authorization code
8. Обменивает код на access token и refresh token
9. Сохраняет токены в базу данных

#### 2. Переавторизация нескольких каналов

```bash
python3 run_youtube_reauth.py "Channel 1" "Channel 2" "Channel 3" --redirect-port 9090
```

Каналы обрабатываются последовательно.

#### 3. Автоматический поиск и переавторизация истекших токенов

```bash
python3 run_youtube_reauth.py --all-expiring --redirect-port 9090
```

Скрипт автоматически найдет все каналы с истекшими токенами и переавторизует их.

#### 4. Headless режим (без GUI браузера)

```bash
python3 run_youtube_reauth.py "Andrew Garle" --redirect-port 9090 --headless
```

**⚠️ Внимание:** В headless режиме может потребоваться ручное вмешательство для MFA.

#### 5. Увеличенный таймаут

```bash
python3 run_youtube_reauth.py "Andrew Garle" --redirect-port 9090 --timeout 600
```

Полезно для медленных соединений или сложных MFA сценариев.

---

## 🤖 Автоматизация

### Что автоматизировано в версии 2.0.0

✅ **Полностью автоматизировано:**

1. **Ввод email** - автоматически заполняется поле email
2. **Ввод пароля** - автоматически заполняется поле пароля
3. **Выбор аккаунта** - автоматически выбирается нужный аккаунт из списка
4. **Пропуск recovery prompts** - автоматически пропускаются запросы на добавление recovery информации
5. **Выбор всех scope** - автоматически выбираются все чекбоксы разрешений
6. **Нажатие кнопки Continue** - **НОВОЕ!** Автоматически находится и нажимается кнопка с `jsname="V67aGc"` ✨
7. **Обмен кода на токены** - автоматически обменивается authorization code на токены
8. **Сохранение токенов** - автоматически сохраняются в базу данных

⚠️ **Требует ручного вмешательства:**

1. **MFA (Multi-Factor Authentication)** - если включена, потребуется ввести код вручную
2. **Security challenges** - дополнительные проверки безопасности от Google
3. **Новые устройства** - первый вход с нового устройства может требовать подтверждения

### Настройка автоматизации MFA (будущая версия)

В будущих версиях планируется поддержка:
- TOTP секретов для автоматического ввода кодов
- Backup codes для автоматического восстановления
- SMS коды (через внешние сервисы)

---

## 🔍 Диагностика и отладка

### Уровни логирования

Скрипт использует стандартный Python logging с уровнями:
- `INFO` - основная информация о процессе
- `DEBUG` - детальная отладочная информация
- `WARNING` - предупреждения
- `ERROR` - ошибки

### Где смотреть логи

1. **Консольный вывод** - основная информация выводится в консоль
2. **Файл логов** - `data/logs/latest.log` или `data/logs/reauth_YYYY-MM-DD.log`

### Примеры логов

#### Успешная переавторизация:

```
2025-11-14 23:03:48,117 - INFO - URL path contains '/consent' - definitely a consent screen
2025-11-14 23:03:48,118 - INFO - Consent screen detected for Andrew Garle, selecting scopes...
2025-11-14 23:03:48,130 - INFO - Calling _click_consent_button for Andrew Garle...
2025-11-14 23:04:07,489 - INFO - Successfully clicked Continue button found by text for Andrew Garle.
2025-11-14 23:04:07,490 - INFO - Successfully clicked consent button for Andrew Garle
2025-11-14 23:04:09,225 - INFO - OAuth reauth completed for Andrew Garle with status success
```

#### Поиск кнопки (детальные логи):

```
2025-11-14 23:03:50,390 - INFO - Method 1: Waiting for element with jsname='V67aGc' to appear...
2025-11-14 23:04:05,396 - INFO - Method 2: Searching for Continue button by text and jsname...
2025-11-14 23:04:07,489 - INFO - Successfully clicked Continue button found by text for Andrew Garle.
```

### Полезные команды для диагностики

#### 1. Проверка статуса токенов

```bash
python run_youtube_manager.py check-tokens
```

#### 2. Проверка лимитов OAuth

```bash
python3 check_token_limit.py
```

#### 3. Просмотр последних логов

```bash
tail -f data/logs/latest.log
```

#### 4. Поиск ошибок в логах

```bash
grep -i error data/logs/latest.log | tail -20
```

---

## 🛠️ Решение проблем

### Проблема 1: Кнопка Continue не нажимается

**Симптомы:**
- Браузер открывается, но кнопка не нажимается автоматически
- В логах: `Could not find Continue button (jsname='V67aGc')`

**Решения:**

1. **Проверьте логи** - какой метод поиска использовался:
   ```bash
   grep "Method" data/logs/latest.log
   ```

2. **Увеличьте таймаут** - возможно элемент появляется с задержкой:
   ```bash
   python3 run_youtube_reauth.py "Channel" --redirect-port 9090 --timeout 600
   ```

3. **Проверьте в браузере** - откройте DevTools и найдите элемент:
   ```javascript
   document.querySelector('div[jsname="V67aGc"]')
   ```

4. **Ручное нажатие** - если автоматизация не работает, нажмите кнопку вручную (скрипт продолжит работу)

### Проблема 2: Consent screen не распознается

**Симптомы:**
- В логах: `Not recognized as consent screen`
- URL содержит `/consent`, но скрипт не продолжает

**Решения:**

1. **Проверьте URL** - должен содержать `/signin/oauth/consent`:
   ```bash
   grep "Page URL" data/logs/latest.log
   ```

2. **Fallback должен сработать** - если URL содержит "consent", скрипт все равно попытается нажать кнопку

3. **Обновите код** - убедитесь что используете последнюю версию из ветки `rpa-auth`

### Проблема 3: Ошибка `'async_generator' object is not iterable`

**Симптомы:**
- Ошибка при запуске: `TypeError: 'async_generator' object is not iterable`

**Решение:**
- Эта ошибка исправлена в версии 2.0.0
- Обновите код до последней версии

### Проблема 4: Порт уже занят

**Симптомы:**
```
OSError: [Errno 48] Address already in use
```

**Решения:**

1. **Используйте другой порт:**
   ```bash
   python3 run_youtube_reauth.py "Channel" --redirect-port 9091
   ```

2. **Освободите порт:**
   ```bash
   # Найти процесс
   lsof -i :9090
   # Убить процесс
   kill -9 <PID>
   ```

3. **Измените Redirect URI** в Google Cloud Console на новый порт

### Проблема 5: MFA требует ручного ввода

**Симптомы:**
- Браузер открывается и ждет ввода MFA кода
- В логах: `MFA challenge encountered. Manual approval required`

**Решения:**

1. **Введите код вручную** - скрипт продолжит работу после ввода кода

2. **Настройте TOTP** (будущая версия):
   - Добавьте TOTP secret в базу данных
   - Скрипт будет автоматически генерировать коды

3. **Используйте backup codes** (будущая версия):
   - Сохраните backup codes в базу данных
   - Скрипт будет использовать их автоматически

### Проблема 6: База данных недоступна

**Симптомы:**
```
ERROR - Failed to connect to MySQL database
```

**Решения:**

1. **Проверьте MySQL:**
   ```bash
   mysql -h 46.21.250.43 -P 3306 -u your_user -p
   ```

2. **Проверьте переменные окружения:**
   ```bash
   echo $MYSQL_HOST
   echo $MYSQL_PORT
   ```

3. **Используйте переменные окружения:**
   ```bash
   MYSQL_HOST=46.21.250.43 MYSQL_PORT=3306 python3 run_youtube_reauth.py "Channel"
   ```

---

## ❓ FAQ

### Q: Как часто нужно переавторизовывать каналы?

**A:** При правильной настройке - никогда. Система автоматически обновляет токены через refresh token. Переавторизация нужна только если:
- Refresh token истек или был отозван
- Токены были удалены из базы данных
- Сменился OAuth Client ID/Secret

### Q: Безопасно ли хранить пароли в базе данных?

**A:** Пароли хранятся в зашифрованном виде. Однако рекомендуется:
- Использовать сильные пароли
- Регулярно менять пароли
- Ограничить доступ к базе данных
- Использовать отдельные OAuth приложения для каждого канала

### Q: Можно ли переавторизовать все каналы одновременно?

**A:** Технически можно, но не рекомендуется:
- Google может заблокировать IP при множественных запросах
- Рекомендуется переавторизовывать по одному или небольшими группами
- Используйте `--all-expiring` для автоматического поиска проблемных каналов

### Q: Что делать если автоматическое нажатие кнопки не работает?

**A:** 
1. Проверьте логи - какой метод поиска использовался
2. Увеличьте таймаут
3. Нажмите кнопку вручную - скрипт продолжит работу
4. Сообщите о проблеме с приложенными логами

### Q: Поддерживаются ли другие языки интерфейса Google?

**A:** Да! Скрипт поддерживает:
- Английский (Continue, Allow)
- Русский (Продолжить, Разрешить)
- Украинский (Продовжити, Дозволити)

Поиск кнопки работает на всех языках.

### Q: Можно ли использовать на сервере без GUI?

**A:** Да, используйте флаг `--headless`:
```bash
python3 run_youtube_reauth.py "Channel" --headless
```

⚠️ Внимание: MFA может потребовать ручного вмешательства даже в headless режиме.

### Q: Как проверить что переавторизация прошла успешно?

**A:** 
1. Проверьте логи - должно быть `status success`
2. Проверьте токены:
   ```bash
   python run_youtube_manager.py check-tokens
   ```
3. Попробуйте загрузить видео - если работает, токены валидны

---

## 📚 Дополнительные ресурсы

- **CHANGELOG:** [REAUTH_CHANGELOG_RPA_AUTH.md](REAUTH_CHANGELOG_RPA_AUTH.md) - детальное описание всех изменений
- **Краткое руководство:** [REAUTH_README.md](REAUTH_README.md) - быстрый старт
- **Техническая документация:** `docs/youtube/05-TOKEN-REAUTH-GUIDE.md`
- **Troubleshooting:** `docs/youtube/04-TROUBLESHOOTING.md`

---

## 🆘 Получить помощь

Если проблема не решена:

1. **Проверьте логи:**
   ```bash
   tail -100 data/logs/latest.log
   ```

2. **Соберите информацию:**
   - Версия Python
   - Версия скрипта (ветка)
   - Последние 50 строк логов
   - Скриншот ошибки (если есть)

3. **Создайте issue** с собранной информацией

---

**Версия документации:** 2.0.0  
**Последнее обновление:** 2025-11-14  
**Автор:** Content Fabric Team


