# Автоматичний лайк та коментар після завантаження відео

## Огляд

Після успішного завантаження відео/shorts на YouTube, система автоматично виконує наступні дії:
1. **Лайкає відео** - ставить лайк на щойно завантажене відео
2. **Додає коментар** - публікує коментар, якщо він був вказаний в БД (поле `post_comment`)

## Як це працює

### 1. Автоматичний лайк

Після успішного завантаження відео, система автоматично викликає YouTube API для того, щоб поставити лайк на відео від імені каналу, який завантажив це відео.

```python
# Автоматично викликається після завантаження
youtube_client.like_video(video_id, account_info)
```

### 2. Автоматичний коментар

Якщо в таблиці `tasks` в полі `post_comment` вказаний текст коментаря, система автоматично публікує його під щойно завантаженим відео.

```python
# Автоматично викликається, якщо є текст коментаря
youtube_client.post_comment(video_id, comment_text, account_info)
```

## Налаштування в БД

### Поле post_comment в таблиці tasks

```sql
-- Приклад додавання таску з коментарем
INSERT INTO tasks (
    account_id,
    media_type,
    title,
    description,
    att_file_path,
    post_comment,  -- Текст коментаря
    date_post
) VALUES (
    1,
    'youtube',
    'Назва відео',
    'Опис відео',
    '/path/to/video.mp4',
    'Це мій перший коментар! 🔥',  -- Коментар буде опублікований
    '2025-01-15 10:00:00'
);
```

### Якщо не потрібен коментар

Просто залиште поле `post_comment` порожнім або `NULL`:

```sql
INSERT INTO tasks (
    account_id,
    media_type,
    title,
    description,
    att_file_path,
    post_comment,  -- NULL або пусто
    date_post
) VALUES (
    1,
    'youtube',
    'Назва відео',
    'Опис відео',
    '/path/to/video.mp4',
    NULL,  -- Коментар НЕ буде опублікований
    '2025-01-15 10:00:00'
);
```

## Логування

Система логує всі дії з лайками та коментарями:

```
INFO: Liking video dQw4w9WgXcQ...
INFO: ✓ Successfully liked video dQw4w9WgXcQ
INFO: Posting comment on video dQw4w9WgXcQ...
INFO: ✓ Successfully posted comment on video dQw4w9WgXcQ
```

Якщо виникає помилка:

```
WARNING: ⚠ Failed to like video dQw4w9WgXcQ
WARNING: ⚠ Failed to post comment on video dQw4w9WgXcQ
ERROR: Failed to like video dQw4w9WgXcQ: Insufficient permissions
```

## Важливо

### YouTube API Scopes

Для роботи лайків та коментарів необхідні наступні API scopes:

```python
SCOPES = [
    'https://www.googleapis.com/auth/youtube.upload',
    'https://www.googleapis.com/auth/youtube',  # Потрібно для лайків та коментарів
]
```

Ці scopes вже додані в `youtube_client.py`, але якщо ви раніше авторизували канал, можливо потрібно буде:
1. Видалити файл `youtube_token.json`
2. Повторно авторизувати канал, щоб отримати нові дозволи

### Обробка помилок

Якщо лайк або коментар не вдається опублікувати, це **не впливає** на успішність завантаження відео. Відео вважається успішно завантаженим навіть якщо лайк/коментар не вдалося опублікувати.

Логіка:
1. ✅ Відео завантажено → задача позначається як виконана
2. ⚠️ Спроба лайкнути → якщо не вдається, логується попередження
3. ⚠️ Спроба коментувати → якщо не вдається, логується попередження

## Нові API методи

### YouTubeClient.like_video()

```python
def like_video(self, video_id: str, account_info: Dict[str, Any]) -> bool:
    """
    Like a video on YouTube.
    
    Args:
        video_id: YouTube video ID
        account_info: Account information with access token
        
    Returns:
        True if successful, False otherwise
    """
```

### YouTubeClient.post_comment()

```python
def post_comment(self, video_id: str, comment_text: str, account_info: Dict[str, Any]) -> bool:
    """
    Post a comment on a YouTube video.
    
    Args:
        video_id: YouTube video ID
        comment_text: Text of the comment to post
        account_info: Account information with access token
        
    Returns:
        True if successful, False otherwise
    """
```

## Приклад використання через task_manager

```python
from scripts.task_manager import TaskManager

manager = TaskManager()

# Створити таск з коментарем
manager.create_task(
    channel_name="My Channel",
    video_path="/path/to/video.mp4",
    title="Amazing Video",
    description="Check this out!",
    comment="Don't forget to subscribe! 👍",  # Коментар буде опублікований
    scheduled_time="2025-01-15 10:00:00"
)
```

## Тестування

Щоб перевірити роботу лайків та коментарів:

1. Створіть тестовий таск з коментарем:
```bash
python run_task_manager.py
```

2. Перевірте логи під час виконання:
```bash
tail -f data/logs/auto_posting.log
```

3. Перевірте відео на YouTube:
   - Чи є лайк від вашого каналу
   - Чи з'явився коментар

## Квота YouTube API

Кожна операція споживає квоту YouTube API:
- **Лайк відео**: 50 units
- **Публікація коментаря**: 50 units
- **Завантаження відео**: 1600 units

**Разом на один таск**: ~1700 units (замість 1600)

Денний ліміт: 10,000 units = приблизно 5-6 відео з лайками та коментарями.

## FAQ

**Q: Чи можна відключити автоматичний лайк?**  
A: Наразі лайк завжди ставиться. Якщо потрібно відключити, потрібно закоментувати відповідний код в `task_worker.py`.

**Q: Що якщо коментар занадто довгий?**  
A: YouTube дозволяє коментарі до 10,000 символів. Система не обрізає текст автоматично.

**Q: Чи можна додати декілька коментарів?**  
A: Наразі підтримується тільки один коментар. Якщо потрібно більше, потрібно розширювати функціонал.

**Q: Помилка "Insufficient permissions"?**  
A: Видаліть `youtube_token.json` та повторно авторизуйте канал з правильними scopes.

