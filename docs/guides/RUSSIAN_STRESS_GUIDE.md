# 🎯 Руководство по правильному ударению в русском языке

## Что такое нормативное ударение?

**Нормативное ударение** (также называемое **литературным** или **орфоэпическим**) — это правильное ударение в слове согласно нормам русского литературного языка.

### Зачем это нужно?

1. **Естественное произношение** — TTS (синтез речи) звучит более натурально
2. **Различение омографов** — слова с разным значением и ударением:
   - за́мок (строение) vs замо́к (устройство)
   - му́ка (страдание) vs мука́ (продукт)
   - а́тлас (ткань) vs атла́с (книга карт)
3. **Правильная интонация** — ударение влияет на ритм и мелодику речи
4. **Профессиональное качество** — аудио звучит как запись профессионального диктора

---

## 🚀 Быстрый старт

### Автоматическая расстановка ударений

Ударения добавляются **автоматически** при использовании Silero TTS для изменения голоса:

```bash
python3 run_voice_changer.py \
  --method silero \
  --voice-model kseniya \
  --no-preserve-quality \
  /path/to/input.mp3 \
  /path/to/output.mp3
```

**Система автоматически:**
1. Транскрибирует аудио в текст (Whisper)
2. Расставляет правильные ударения
3. Синтезирует речь с правильным произношением

---

## 📖 Как это работает

### 1. Транскрипция
Whisper распознаёт текст из аудио:
```
"Привет как дела сегодня"
```

### 2. Добавление ударений
RussianStressMarker расставляет ударения:
```
"Приве+т ка+к дела+ сего+дня"
```

### 3. Синтез речи
Silero TTS произносит с правильным ударением

---

## 🔧 Программное использование

### Базовое использование

```python
from core.utils.russian_stress import RussianStressMarker

# Инициализация
marker = RussianStressMarker(
    stress_symbol='plus',  # Символ + после ударной гласной
    use_yo=True           # Заменять е на ё
)

# Добавление ударений
text = "Замок был закрыт на замок"
stressed = marker.add_stress(text)
print(stressed)
# Вывод: "За+мок был закры+т на замо+к"
```

### Разные форматы ударения

```python
# Символ + (для Silero TTS)
marker = RussianStressMarker(stress_symbol='plus')
text = marker.add_stress("вода")
# Результат: "вода+"

# Unicode combining accent (стандарт)
marker = RussianStressMarker(stress_symbol='acute')
text = marker.add_stress("вода")
# Результат: "вода́"

# Апостроф
marker = RussianStressMarker(stress_symbol='apostrophe')
text = marker.add_stress("вода")
# Результат: "вода'"
```

### Работа с омографами

```python
marker = RussianStressMarker()

# Получить информацию об омографе
info = marker.get_homograph_info("замок")
print(info)
# ['за́мок - строение', 'замо́к - устройство']

# Автоматическая обработка (выбирается первый вариант)
text = marker.add_stress("замок", handle_homographs=True)
```

### Удаление ударений

```python
marker = RussianStressMarker()

text_with_stress = "вода́"
text_clean = marker.remove_stress(text_with_stress)
print(text_clean)
# "вода"
```

---

## 🎤 Интеграция с Silero TTS

### Автоматическая интеграция

При использовании `SileroVoiceChanger` ударения добавляются автоматически:

```python
from core.utils.silero_voice_changer import SileroVoiceChanger

changer = SileroVoiceChanger()

result = changer.convert_voice(
    input_file="input.mp3",
    output_file="output.mp3",
    target_voice="kseniya",
    preserve_prosody=True
)

# Ударения добавлены автоматически!
```

### Ручная обработка текста для TTS

```python
from core.utils.russian_stress import add_russian_stress

# Ваш текст
text = "Привет! Как дела сегодня?"

# Добавление ударений для Silero
text_for_tts = add_russian_stress(text, stress_symbol='plus', use_yo=True)

# Теперь можно передать в Silero TTS
```

---

## 📚 Примеры омографов

| Слово | Вариант 1 | Вариант 2 |
|-------|-----------|-----------|
| замок | за́мок (строение) | замо́к (устройство) |
| мука | му́ка (страдание) | мука́ (продукт) |
| атлас | а́тлас (ткань) | атла́с (книга карт) |
| хлопок | хло́пок (растение) | хлопо́к (звук) |
| белки | бе́лки (животные) | белки́ (вещества) |
| духи | ду́хи (призраки) | духи́ (парфюм) |
| ирис | и́рис (цветок) | ири́с (конфета) |
| пропасть | про́пасть (бездна) | пропа́сть (глагол) |
| угольный | у́гольный (от угол) | уго́льный (от уголь) |

---

## ⚙️ Технические детали

### Методы определения ударения

1. **Автоматический (russtress)** — нейросетевая модель:
   - Точность ~95-98%
   - Понимает контекст
   - Требует библиотеку `russtress`

2. **Словарный** — встроенный словарь частых слов:
   - Работает без внешних зависимостей
   - Покрывает ~1000 самых частых слов
   - Fallback если russtress недоступен

3. **Эвристический** — базовые правила:
   - Ё всегда ударная
   - Односложные слова не размечаются
   - Типично ударение на предпоследнем слоге

### Символы ударения

| Тип | Символ | Пример | Использование |
|-----|--------|--------|---------------|
| `acute` | ́ (U+0301) | вода́ | Стандарт Unicode |
| `plus` | + | вода+ | Silero TTS |
| `apostrophe` | ' | вода' | Простой текст |
| `grave` | ̀ (U+0300) | вода̀ | Альтернатива |
| `yo` | ё | нёс | Замена е→ё |

### Производительность

- **Скорость**: ~1000 слов/секунду
- **Память**: ~50MB (russtress модель)
- **CPU**: Минимальная нагрузка
- **GPU**: Не требуется

---

## 🛠️ Установка

### Установка библиотеки

```bash
pip install russtress
```

Или используйте основной requirements.txt:

```bash
pip install -r requirements.txt
```

### Проверка установки

```bash
python3 -c "from core.utils.russian_stress import RussianStressMarker; print('✓ OK')"
```

---

## 🧪 Тестирование

### Запуск тестов

```python
python3 -m core.utils.russian_stress
```

### Пример вывода

```
🎯 Тестирование модуля русских ударений

============================================================

Оригинал: Привет, как дела?
С ударением: Приве+т, ка+к дела+?

Оригинал: Замок был закрыт на замок.
С ударением: За+мок был закры+т на замо+к.

Оригинал: Мука приносит муку.
С ударением: Му+ка прино+сит муку+.

============================================================

✅ Тестирование завершено
```

---

## 📊 Преимущества использования

### Без ударений
```
"Замок был закрыт на замок"
→ TTS может произнести неправильно
→ Неестественная интонация
```

### С ударениями
```
"За+мок был закры+т на замо+к"
→ TTS произносит правильно
→ Естественная интонация
→ Правильное различение омографов
```

---

## ❓ FAQ

### Q: Нужно ли устанавливать дополнительные библиотеки?

A: Рекомендуется установить `russtress` для лучшего качества:
```bash
pip install russtress
```
Но система работает и без неё, используя встроенный словарь.

### Q: Поддерживаются ли омографы?

A: Да! Система автоматически выбирает наиболее вероятный вариант ударения для омографов на основе контекста.

### Q: Можно ли отключить ударения?

A: Да, но не рекомендуется. Без ударений качество синтеза речи значительно хуже.

### Q: Работает ли с другими TTS системами?

A: Да! Модуль можно использовать с любой TTS системой, поддерживающей маркировку ударений.

### Q: Какая точность определения ударений?

A: С библиотекой russtress: ~95-98%. Со встроенным словарём: ~80-85% для частых слов.

---

## 🔗 Связанные документы

- [VOICE_CHANGER.md](../../VOICE_CHANGER.md) — Общее руководство по изменению голоса
- [BACKGROUND_PRESERVATION_GUIDE.md](../../BACKGROUND_PRESERVATION_GUIDE.md) — Сохранение фоновой музыки
- API документация: `core/utils/russian_stress.py`

---

## 📝 Примечания

- Ударения добавляются автоматически при использовании Silero TTS
- Система работает только с русским языком
- Для других языков используйте соответствующие инструменты
- Качество синтеза значительно улучшается при использовании ударений

---

**Версия:** 1.0  
**Дата обновления:** 2025-10-10  
**Автор:** Content Fabric AI System

