# 🔐 Переавторизация YouTube каналов - Краткое руководство

> Быстрое решение проблем с expired/revoked OAuth токенами

## 🚨 Когда использовать?

Вы видите эту ошибку:
```
ERROR - invalid_grant: Token has been expired or revoked
```

## ⚡ Быстрое решение

### Шаг 1: Проверьте проблемные каналы
```bash
python run_youtube_manager.py check-tokens
```

### Шаг 2: Переавторизуйте

#### Один канал
```bash
python3 reauth_multiple_channels.py audiokniga-one
```

#### Все каналы с истекшими токенами
```bash
python3 reauth_multiple_channels.py --expired
```

#### Несколько конкретных каналов
```bash
python3 reauth_multiple_channels.py channel-1 channel-2 channel-3
```

### Шаг 3: Проверьте результат
```bash
python run_youtube_manager.py check-tokens
# Должно показать: ✅ Токен действителен
```

---

## 📋 Доступные команды

| Команда | Описание |
|---------|----------|
| `python3 reauth_multiple_channels.py CHANNEL` | Переавторизовать один канал |
| `python3 reauth_multiple_channels.py --expired` | Автопоиск и переавторизация истекших |
| `python3 check_token_limit.py` | Проверка лимитов Google OAuth |
| `python run_youtube_manager.py check-tokens` | Проверка статуса всех токенов |

---

## 🔍 Диагностика лимитов

Если переавторизация не помогает, проверьте лимиты:

```bash
python3 check_token_limit.py

# Показывает:
# - Сколько каналов используют один Client ID
# - Предупреждения о возможном превышении лимита (50 токенов)
# - Рекомендации по исправлению
```

---

## 📚 Полная документация

Детальное руководство: [docs/youtube/05-TOKEN-REAUTH-GUIDE.md](docs/youtube/05-TOKEN-REAUTH-GUIDE.md)

Включает:
- ✅ Причины истечения токенов
- ✅ Детальное использование скриптов
- ✅ Решение всех возможных проблем
- ✅ Лучшие практики
- ✅ FAQ

---

## 💡 Частые вопросы

**Q: Как часто нужно переавторизовывать?**  
A: При правильной настройке - никогда. Код автоматически обновляет токены.

**Q: Потеряю ли я данные?**  
A: Нет. Переавторизация только обновляет токены доступа.

**Q: Можно ли переавторизовать все каналы сразу?**  
A: Можно, но лучше только проблемные: `--expired`

---

## 🆘 Проблемы?

1. Проверьте что MySQL/Docker запущен
2. Проверьте логи: `tail -f data/logs/latest.log`
3. Убедитесь что redirect URI = `http://localhost:8080/callback`
4. См. [04-TROUBLESHOOTING.md](docs/youtube/04-TROUBLESHOOTING.md)

---

**Версия:** 1.0 | **Дата:** 2025-10-21

