# Выполненные задачи — 28.02.2026

## API Endpoints (новые)

1. POST /auth/2fa/setup — настройка двухфакторной аутентификации (TOTP)
2. POST /auth/2fa/verify — подтверждение 2FA кодом из authenticator
3. POST /auth/2fa/disable — отключение 2FA (с подтверждением пароля)
4. POST /tasks/batch — пакетная загрузка до 100 задач за раз
5. GET /tasks/calendar — задачи сгруппированные по дням
6. GET /tasks/stats/summary — агрегация задач по статусам
7. GET /tasks/{id}/progress — Redis-backed прогресс загрузки (stage + %)
8. GET /tasks/{id}/preview — инфо о файле + YouTube URL
9. PUT /tasks/{id} — перепланирование задачи (смена даты/времени)
10. POST /tasks/{id}/cancel — отмена запланированной публикации
11. GET /tasks/history — история завершённых/ошибочных/отменённых задач
12. CRUD /templates/ — шаблоны расписаний с time slots по дням недели
13. POST /templates/{id}/slots — добавление слота в шаблон
14. DELETE /templates/{id}/slots/{slot_id} — удаление слота
15. GET /channels/{id}/stats — ежедневная статистика канала
16. GET /admin/dashboard — обзор системы (каунты, очереди)
17. GET /admin/users — все пользователи с статистикой
18. GET /admin/queue — статус Redis очередей
19. POST /uploads/video — загрузка видео (mp4, mkv, avi и т.д., до 10GB)
20. POST /uploads/thumbnail — загрузка превью (jpg, png, webp, до 50MB)

## Security

21. Rate limiting — 10 req/min логин, 5 req/min регистрация, 120/min default
22. Security headers — X-Frame-Options, X-Content-Type-Options, X-XSS-Protection, Referrer-Policy
23. CORS ограничены — только нужные методы и заголовки
24. Path traversal protection — блокировка `..` в file paths
25. Error sanitization — никаких stack traces в ответах
26. Swagger /docs скрыт при DEBUG=False (потом включили обратно)
27. Audit logging — все auth events + task operations → /var/log/cff-audit.log
28. 2FA backup codes — 8 одноразовых кодов при включении TOTP

## SSR Admin Panel (/panel/)

29. Dashboard — каналы (active/total), юзеры, задачи (pending/completed/failed), очередь Redis, ошибки, реавторизация
30. Channels — список всех каналов с token status
31. Tasks — список задач + статистика карточками
32. Users — список пользователей со статусами и 2FA
33. Credentials — login credentials каналов (email, TOTP, proxy, last error)
34. Payment — заглушка с тарифами (Free/Pro $29/Enterprise $99)
35. Защита Admin Panel — все /panel/ страницы требуют cookie + admin роль

## User Portal (/app/)

36. Login page — email + password + optional TOTP code
37. Register page — username, email, display name, password
38. Cookie-based JWT auth — HttpOnly cookie, 24h expiry, SameSite=Lax
39. Dashboard — карточки статистики + 10 последних задач + quick actions
40. Channels page — таблица каналов + кнопка Add Channel
41. Add Channel form — name, YouTube ID, OAuth console, RPA credentials (email, password, TOTP)
42. Tasks page — фильтры по статусу/каналу + таблица задач с badges
43. Create Task form — channel, title, file path, scheduled_at, description, keywords, thumbnail, comment
44. Schedule Templates page — список шаблонов с slot counts
45. Account Settings — профиль + 2FA статус
46. Logout — очистка cookie + redirect
47. Role separation — admin видит /panel/ + /app/, user только /app/

## Тесты

48. Написано 91 тестов (security, repos, API channels/tasks/auth/admin/templates/uploads, upload logic)
49. Все тесты проходят на проде (Linux)
50. pytest.ini + requirements-test.txt + conftest.py с shared fixtures

## Systemd

51. cff-api.service — FastAPI + SSR
52. cff-scheduler.service — DB poller
53. cff-publishing-worker.service — YouTube upload worker
54. cff-notification-worker.service — Telegram/email worker
55. cff-voice-worker.service — voice change worker (stub)

## Прод: каналы и процессы

56. Детальный анализ всех проблемных каналов — 4 категории ошибок из reauth логов
57. Починили knigbaza + knigovabaza (processing_status 0 → 1)
58. Убили legacy task_worker на проде
59. Убили 4 зомби-процесса реавторизации
60. Подтвердили 17 из 30 enabled каналов рабочие

## Nginx

61. Починили конфликт FASTPANEL parking.conf vs content-fabric.conf (listen IP specificity)
62. API и портал доступны по http://46.21.250.43

## Баг-фиксы

63. /tasks/history 500 — IntEnum не сериализовался в mysql-connector (передавал `<TaskStatus.COMPLETED: 1>` вместо `1`)
64. httpx 0.28 несовместим с starlette 0.35 на проде — зафиксировали httpx<0.28
65. Включили Swagger /docs на проде (было скрыто)

## GitHub Issues (созданы и назначены)

66. #96 — Реавторизация 9 каналов через VNC (@mykytatishkin)
67. #97 — Канали новин, test user в GCP (@mykytatishkin)
68. #98 — Топ шоу ру, проверить email (@mykytatishkin)
69. #99 — TOTP секреты от команды (@mykytatishkin)
70. #100 — Voice worker ML портирование (@mykytatishkin) + детальный коммент
71. #101 — Подключение оплаты (@mykytatishkin) + детальный коммент
72. #102 — YouTube LIVE стриминг (@mykytatishkin)
73. #104 — C++ модуль ватермарки/субтитры (@graf_crayt Дима) + детальный коммент
74. #105 — SSL/HTTPS сертификаты Let's Encrypt (@mykytatishkin)
75. #106 — Настройка доменного имени (@mykytatishkin)

## Документация

76. docs/PORTAL_GUIDE.md — полная документация User Portal + Admin Panel
77. docs/REPORT_PMA.md — отчёт для ПМ (7 фаз, все задачи, риски, приоритеты)
78. docs/REPORT_DEV.md — техническая документация (архитектура, endpoints, деплой, тесты, security)
79. Voice worker changer.py — детальный TODO с инструкциями по ML портированию

## Тестовые аккаунты на проде

80. admin@cff.local / Admin123! — админ (доступ ко всему)
81. demo@cff.local / Demo123! — юзер (только /app/)

## Закрытые/удалённые задачи

82. Instagram — закрыта (не интересует пока)
83. CI/CD — закрыта (руками справляемся)
84. YouTube Shorts — закрыта (уже есть)

## Портал: расширение (01.03.2026)

85. Channel Detail page — `/app/channels/{id}` со статистикой задач, credentials, recent tasks
86. Channel Edit page — `/app/channels/{id}/edit` с формой: rename, toggle enabled, RPA credentials
87. Delete Channel — кнопка удаления на списке и детальной странице с confirm dialog
88. Task Detail page — `/app/tasks/{id}` с полной информацией, reschedule формой
89. Cancel Task — кнопка отмены pending задач на списке и детальной странице
90. Retry Task — кнопка повтора failed/cancelled задач (reset to pending, increment retry_count)
91. File Upload в Create Task — AJAX-загрузка видео + thumbnail из браузера, автозаполнение пути
92. Profile Edit — форма display name + timezone на /app/settings
93. 2FA UI — полный flow через портал: setup → TOTP secret → verify code → backup codes → disable
94. Password Change — смена пароля с валидацией (current, new, confirm)
95. Template CRUD — создание шаблонов через портал, детальная страница со слотами
96. Template Slots — добавление/удаление time slots (день, время, канал)
97. Dashboard v2 — active/total channels, success rate %, upcoming tasks, channel names, clickable links
98. Кликабельные ссылки — имена каналов/задач ведут на детальные страницы
99. channel_repo: update_channel(), update_login_credentials(), delete_channel()
100. task_repo: retry_task()
101. user_repo: update_profile(), change_password()

## Безопасность и изоляция данных (28.02.2026, сессия 2)

102. User-scoped data filtering — `_user_filter()` хелпер: admin видит всё, user — только свои данные
103. Фильтрация каналов по `created_by` на dashboard, channels list, task new dropdown
104. Фильтрация задач по `created_by` на dashboard, tasks list, task detail
105. Фильтрация шаблонов по `created_by` на templates list
106. Access control — проверка ownership на channel detail/edit/delete
107. Access control — проверка ownership на task detail/cancel/retry/reschedule
108. Access control — проверка ownership на template detail/slots/delete
109. `created_by=user["id"]` при создании каналов и задач через портал
110. Fix `_is_admin()` — сравнение `status == 1` вместо `status == "admin"` (DB хранит int)
111. Fix `panel.py` admin check — аналогичная ошибка с типом
112. UUID колонки в БД — `uuid VARCHAR(36) NOT NULL UNIQUE` в platform_channels, content_upload_queue_tasks, schedule_templates
113. `get_channel_by_uuid()` — поиск канала по UUID
114. `get_task_by_uuid()` — поиск задачи по UUID
115. `get_template_by_uuid()` — поиск шаблона по UUID
116. UUID генерация при INSERT: channels, tasks (обычные + batch), templates
117. Все 12 portal routes заменены с `{int_id}` на `{uuid}` в URL
118. Все 8 HTML шаблонов обновлены: ссылки используют UUID
119. `get_slots()` с JOIN для channel_uuid/channel_name вместо "Channel #id"
120. DB миграция на проде: ALTER TABLE + backfill UUID() + UNIQUE index (3 таблицы, 5794 записи)
121. Полное тестирование на проде: 23 теста (login, pages, UUID links, IDOR, access control, data isolation)
122. Документация обновлена: PORTAL_GUIDE, REPORT_PMA, REPORT_DEV, PROD_README, SCHEMA_INDEX, DEPLOYMENT

## Legacy cleanup (28.02.2026, сессия 3)

123. Удалены 11 legacy DDL файлов из database/DDL/ (google_consoles_ddl.sql, youtube_channels_ddl.sql, etc.)
124. Удалён docker/init/01-legacy-schema.sql (legacy dev Docker init)
125. Удалён prod/migrations/add_user_id_to_youtube_channels.sql (заброшенная миграция)
126. Удалены 220+ файлов из legacy/ (app, config, docs, scripts, tests, data, examples, queries)
127. Оставлены только непортированные модули: core/voice/ и core/cpp/video/
128. legacy/ переименован в unported/ для ясности
129. Удалена секция "Legacy Table Mapping" из SCHEMA_INDEX.md
130. Удалена секция "Table Mapping Reference" из DEPLOYMENT.md
131. Удалена ссылка на PR #71 (legacy code changes) из DEPLOYMENT.md
132. Удалён backward-compat alias `list_google_consoles` из channel_repository.py
133. Обновлён TODO в voice/changer.py: пути legacy/ → unported/
134. Удалён коммент "legacy worker" из scheduler/jobs.py
135. Исправлены мёртвые ссылки на legacy/ в SERVER_START_GUIDE.md
136. Создан GitHub issue #107: портирование unported/ модулей в prod
137. Итог: 289 файлов, -61163 строк, репо стало на ~27MB легче

## Security Pentest & Remediation (02.03.2026)

138. **[CRITICAL]** Channels API — все 6 endpoints работали БЕЗ авторизации → добавлен `Depends(get_current_user)`
139. **[CRITICAL]** CORS wildcard `["*"]` + `allow_credentials=True` → ограничен до `["http://46.21.250.43"]`, credentials отключены
140. **[CRITICAL]** JWT fallback key `"insecure-dev-only-key"` → `raise RuntimeError` при отсутствии env var
141. **[CRITICAL]** Demo endpoint `/api/v1/items/*` без auth → удалён items.py + router
142. **[HIGH]** Task IDOR — все API tasks без user-scoped filtering → добавлены `_scoped_user_id()` + `_check_task_owner()` на все endpoints
143. **[HIGH]** Path traversal в `_file_info()` → валидация что path начинается с `UPLOAD_DIR`
144. **[MEDIUM]** Security headers — добавлены CSP, Permissions-Policy
145. **[MEDIUM]** Rate limits — uploads 10/min, channels create 10/min, tasks create 20/min, batch 5/min
146. **[MEDIUM]** Password minimum 6→8 символов
147. **[MEDIUM]** Backup codes entropy: `token_hex(4)` → `token_hex(6)` (32→48 бит)
148. **[MEDIUM]** uvicorn bind `0.0.0.0` → `127.0.0.1`
149. **[WARNING]** `datetime.utcnow()` → `datetime.now(UTC)` в security.py, audit.py, app_portal.py, token_refresh.py

## Ops, Logging, Refactoring & Server Hardening (02.03.2026, сессия 2)

150. **Dependencies** — обновлены до последних: fastapi 0.115.12, uvicorn 0.34.2, pydantic 2.11.3, pydantic-settings 2.9.1, mysql-connector 9.3.0, google-api 2.169.0, jinja2 3.1.6
151. **Python 3.10 compat** — `datetime.UTC` (3.11+) заменён на `datetime.timezone.utc` в security.py, audit.py, app_portal.py, token_refresh.py
152. **Task data mismatch fix** — API `create_task`/`create_tasks_batch` не передавали `created_by`, panel dashboard не показывал processing/cancelled задачи, `completed_at` NULL у failed задач
153. **Centralized auth** — создан `app/core/auth.py`, консолидированы 4 дубликата auth-функций из panel.py, app_portal.py, tasks.py, admin.py
154. **Task column dedup** — `_task_cols()` используется последовательно во всех запросах task_repo вместо inline копий
155. **Systemd services** — bind 127.0.0.1, `-/` prefix для env files, MemoryMax (1G API, 2G pub, 4G voice, 512M rest), LimitNOFILE=65536, SyslogIdentifier, StartLimitBurst
156. **Logrotate** — daily rotation (14 дней), weekly audit (52 недели)
157. **Nginx** — upstream keepalive, gzip, security headers, health bypass, static alias, 10G client_max_body_size
158. **orjson** — добавлен как зависимость для быстрой JSON-сериализации
159. **Detailed logging** — добавлено в 11 файлов: user_repo (create, password, 2FA), credential_repo (add, upsert, RPA, TOTP), channel_repo (add, tokens, delete), console_repo, audit_repo, stats_repo, auth.py (login/register/2FA), admin.py, templates.py, queue config/publisher
160. **Server packages** — apt upgrade 50 пакетов: nginx 1.28.0→1.28.2, MySQL 8.0.43→8.0.44, systemd, gcc, ldap security patches
161. **MySQL GPG key** — обновлён протухший ключ B7B3B788A8D3785C

---

# Что делать дальше

## Ручные задачи (Никита @mykytatishkin)

1. **Домен + SSL** (issues #106, #105) — купить домен, настроить DNS A-запись → 46.21.250.43, установить certbot, получить Let's Encrypt сертификат, обновить nginx для HTTPS
2. **Реавторизация 9 каналов** (issue #96) — зайти на сервер через VNC, залогиниться в Google в браузере для каждого аккаунта, пройти "Verify it's you" security challenge (~5 мин/аккаунт)
3. **Канали новин** (issue #97) — добавить kanalinovin831@gmail.com в test users в Google Cloud Console
4. **Топ шоу ру** (issue #98) — проверить email topsooru@gmail.com, Google не находит аккаунт
5. **TOTP секреты** (issue #99) — получить от команды секреты для каналов с 2FA
6. **Voice worker** (issue #100) — портировать unported/core/voice/voice_changer.py в prod, установить torch/librosa/pyworld/so-vits-svc, скачать voice models, протестировать
7. **Оплата** (issue #101) — зарегистрироваться в Stripe/LiqPay, создать таблицы subscriptions/payment_history, добавить API endpoints, подключить webhook
8. **YouTube LIVE** (issue #102) — реализовать стриминг через YouTube Live API
9. **Google OAuth верификация** (issue #19) — подать заявку на верификацию OAuth app в Google (долгий процесс)
10. **MySQL backup** — настроить cron + mysqldump → хранилище

## Задача на Диму (@graf_crayt)

11. **C++ модуль** (issue #104) — удаление ватермарков (OpenCV), удаление субтитров (OCR + inpainting), нанесение своих субтитров (.srt/.ass), Python binding через subprocess или pybind11, GPU Quadro P2000 доступен

## Задачи на разработку (следующая сессия)

12. ~~**Статистика по каналам**~~ — DONE: `/app/channels/{id}` со статистикой задач
13. **Готовые UI блоки** — Сергей хочет предложить свой дизайн через готовые блоки, дождаться его макетов
14. ~~**Улучшение портала**~~ — DONE: channel detail/edit, task detail, retry, file upload, template CRUD, dashboard v2
15. ~~**Фильтрация по user**~~ — DONE: `_user_filter()` + `created_by` фильтрация + access control на всех маршрутах
16. ~~**2FA в портале**~~ — DONE: полный UI flow на /app/settings
17. ~~**Редактирование профиля**~~ — DONE: display_name + timezone на /app/settings
18. ~~**Удаление канала**~~ — DONE: кнопка на /app/channels и /app/channels/{id}
19. ~~**Отмена задачи**~~ — DONE: Cancel на /app/tasks + /app/tasks/{id}
20. ~~**Загрузка файлов через портал**~~ — DONE: AJAX upload видео и thumbnail в /app/tasks/new

## Приоритет

**Критически важно (сначала):**
- Домен + SSL (#105, #106) — без HTTPS токены летят в открытом виде
- Реавторизация 9 каналов (#96) — больше половины enabled каналов не работают

**Важно (потом):**
- Готовые UI блоки от Сергея
- Voice worker ML porting

**Когда будет время:**
- Оплата, YouTube LIVE, Google OAuth верификация
- C++ модуль (Дима работает параллельно)
