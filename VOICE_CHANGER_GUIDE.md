# 🎙️ Voice Changer - Руководство по использованию

## 📋 Что это?

Voice Changer для русского языка с использованием Silero TTS и Whisper.

**Особенности:**
- ✅ Естественный русский голос (6 вариантов)
- ✅ Сохранение пауз из оригинала
- ✅ Улучшенная транскрипция (Whisper medium)
- ✅ Бесплатно и работает локально

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip3 install -r requirements.txt
```

### 2. Простое использование

```bash
python3 run_voice_changer.py \
  --method silero \
  --voice-model kseniya \
  --no-preserve-quality \
  input.mp3 output.mp3
```

**Время обработки:** ~5-7 минут для 3-минутного аудио

---

## 🎤 Доступные голоса

Посмотреть все голоса:

```bash
python3 run_voice_changer.py --list-silero-voices
```

### Русские голоса Silero:

| Голос | Пол | Описание |
|-------|-----|----------|
| `kseniya` | Ж | Ксения - женский голос |
| `baya` | Ж | Бая - женский голос |
| `xenia` | Ж | Ксения (вариант 2) |
| `aidar` | М | Айдар - мужской голос |
| `eugene` | М | Евгений - мужской голос |
| `random` | ? | Случайный русский голос |

**Рекомендация:** `kseniya` или `baya` для женского, `eugene` для мужского

---

## 📖 Примеры команд

### Базовое использование

```bash
# Женский голос (Ксения)
python3 run_voice_changer.py \
  --method silero \
  --voice-model kseniya \
  --no-preserve-quality \
  data/content/audio/input.mp3 \
  data/content/audio/output_kseniya.mp3

# Мужской голос (Евгений)
python3 run_voice_changer.py \
  --method silero \
  --voice-model eugene \
  --no-preserve-quality \
  data/content/audio/input.mp3 \
  data/content/audio/output_eugene.mp3
```

### Обработка видео

```bash
# Voice changer автоматически извлечет аудио, обработает и вернет видео
python3 run_voice_changer.py \
  --method silero \
  --voice-model kseniya \
  --no-preserve-quality \
  input.mp4 \
  output.mp4
```

---

## 🔧 Параметры

### Обязательные:

```bash
python3 run_voice_changer.py INPUT OUTPUT
```

- `INPUT` - входной файл (mp3, mp4, wav, etc.)
- `OUTPUT` - выходной файл

### Опциональные:

| Параметр | По умолчанию | Описание |
|----------|--------------|----------|
| `--method silero` | `sovits` | Использовать Silero TTS (для русского) |
| `--voice-model VOICE` | `kseniya` | Выбор голоса |
| `--no-preserve-quality` | `False` | Быстрее, без prosody transfer |

---

## ⚙️ Как это работает

### 1. **Транскрипция** (Whisper medium)
```
input.mp3 → Whisper → "Колобок жили-были старик со старухой..."
                   + timestamps для каждого сегмента
```

### 2. **Улучшение текста**
```
"Колобок жили-были старик со старухой..." 
→ Пунктуация, капитализация
→ "Колобок. Жили-были старик со старухой. Однажды..."
```

### 3. **Синтез с паузами** (Silero TTS)
```
Сегмент 1: "Колобок" → синтез
Пауза: 1.42s
Сегмент 2: "Жили-были старик со старухой" → синтез
Пауза: 0.50s
...
```

### 4. **Сохранение**
```
Все сегменты + паузы → output.mp3
```

---

## 📊 Качество и ограничения

### ✅ Что работает хорошо:

- Четкая речь
- Естественные паузы
- Правильная пунктуация
- Стандартное произношение

### ⚠️ Ограничения:

- **Ударения:** ~70-80% правильно (автоматические)
- **Эмоции:** Нейтральная интонация
- **Акценты:** Только стандартный русский
- **Скорость:** TTS всегда одинаковая

### 💡 Улучшение качества:

1. **Четкая запись** - меньше фонового шума
2. **Хорошая дикция** - Whisper лучше распознает
3. **Женский голос** - обычно звучит естественнее

---

## 🎯 Сравнение методов

У вас есть 2 метода:

### Метод 1: So-VITS-SVC (Voice-to-Voice)

```bash
python3 run_voice_changer.py \
  --voice-model female_hq_48k \
  input.mp3 output.mp3
```

**Плюсы:**
- Сохраняет интонации
- Быстрее

**Минусы:**
- Роботизированный звук
- Нужны обученные модели

### Метод 2: Silero TTS ⭐ (Рекомендовано)

```bash
python3 run_voice_changer.py \
  --method silero \
  --voice-model kseniya \
  --no-preserve-quality \
  input.mp3 output.mp3
```

**Плюсы:**
- Естественный русский голос
- Правильные паузы
- Хорошая транскрипция

**Минусы:**
- Медленнее (5-7 мин)
- TTS (синтезирует заново)
- Ударения могут быть неправильные

---

## 🔍 Решение проблем

### Проблема: "ModuleNotFoundError"

```bash
pip3 install -r requirements.txt
```

### Проблема: "Whisper model not loaded"

Модель скачается автоматически при первом запуске. Подождите ~1-2 минуты.

### Проблема: "Слишком медленно"

1. Используйте `--no-preserve-quality` (уже в примерах)
2. Убедитесь что используете `--method silero`
3. Первый запуск всегда медленнее (загрузка моделей)

### Проблема: "Неправильные ударения"

Это известное ограничение. Silero делает ~70-80% правильно.

**Решения:**
- Принять как есть (для большинства случаев достаточно)
- Использовать платный API (ElevenLabs, Play.ht)

### Проблема: "Роботизированный голос"

Убедитесь что используете `--method silero`, а не So-VITS.

---

## 📁 Структура проекта

```
content-fabric/
├── run_voice_changer.py          # Основной скрипт
├── core/
│   └── utils/
│       ├── voice_changer.py      # Главный класс
│       ├── silero_voice_changer.py  # Silero TTS
│       ├── prosody_transfer.py   # Передача просодии
│       └── rvc_inference.py      # RVC inference
├── data/
│   └── content/
│       └── audio/                # Ваши аудио файлы
└── requirements.txt              # Зависимости
```

---

## 💾 Примеры результатов

В папке `data/content/audio/` вы найдете примеры:

```bash
# Примеры с разными голосами (старые, без пауз):
sokol/test_aidar.mp3      # Айдар (муж)
sokol/test_baya.mp3       # Бая (жен)
sokol/test_eugene.mp3     # Евгений (муж)
sokol/test_xenia.mp3      # Ксения v2 (жен)

# Финальная итерация (с паузами):
voice_kseniya_pauses.mp3  # ⭐ Лучший результат
```

---

## 🎓 Продвинутое использование

### Обработка нескольких файлов

```bash
for file in data/content/audio/*.mp3; do
  output="${file%.mp3}_kseniya.mp3"
  python3 run_voice_changer.py \
    --method silero \
    --voice-model kseniya \
    --no-preserve-quality \
    "$file" "$output"
done
```

### Тестирование всех голосов

```bash
voices=("kseniya" "baya" "aidar" "eugene" "xenia")

for voice in "${voices[@]}"; do
  echo "Processing with voice: $voice"
  python3 run_voice_changer.py \
    --method silero \
    --voice-model "$voice" \
    --no-preserve-quality \
    input.mp3 "output_${voice}.mp3"
done
```

---

## 🔄 Сравнение с оригиналом

### Оригинал:
- Любой голос
- Любые эмоции
- Любые ударения

### Voice Changer (Silero):
- ✅ Новый голос (один из 6)
- ⚠️ Нейтральная интонация
- ⚠️ ~70-80% правильных ударений
- ✅ Правильные паузы
- ✅ Хорошая дикция

**Вывод:** Подходит для:
- Озвучка контента
- Замена голоса для анонимности
- Тестовые версии
- Черновики

**НЕ подходит для:**
- Профессиональные аудиокниги
- Коммерческая озвучка высокого качества
- Эмоциональный контент

---

## 💡 Альтернативы

Если качество не устраивает:

### Бесплатные:
1. **RVC с обученными моделями** - найти готовые модели (сложно)
2. **Coqui TTS** - другой TTS движок (похожее качество)

### Платные:
1. **ElevenLabs** ($5-10/мес) - лучшее качество
2. **Play.ht** ($9/мес) - хорошее качество
3. **Murf.ai** ($19/мес) - профессиональное качество

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте `requirements.txt` установлен
2. Убедитесь что используете Python 3.8+
3. Проверьте логи в консоли
4. Попробуйте другой голос

---

## 🎯 Итоговая рекомендация

**Для большинства случаев используйте:**

```bash
python3 run_voice_changer.py \
  --method silero \
  --voice-model kseniya \
  --no-preserve-quality \
  input.mp3 output.mp3
```

**Результат:**
- Естественный русский женский голос
- Правильные паузы из оригинала
- ~70-80% правильных ударений
- Обработка за 5-7 минут

**Это лучшее что можно получить бесплатно! 🎉**

---

## 📝 История версий

- **v1.0** - Praat (pitch/formant shifting)
- **v2.0** - RVC integration
- **v3.0** - So-VITS-SVC
- **v4.0** - Silero TTS для русского ⭐
- **v4.1** - Whisper medium (улучшенная транскрипция)
- **v4.2** - Автоматические паузы из timestamps ⭐ **← Вы здесь**

---

**Удачной работы! 🚀**

